<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>All Recipes</title>
  </head>
  <body>
    <h2>All Recipes</h2>
      <% @recipes.each do |recipe| %>
        <h3><a href="/recipes/<%= recipe.id %>"></a><%= recipe.name %></h3>
      <% end %>
  </body>
</html>
